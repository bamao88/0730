# 模板文字分类

先问“它在当前学校材料中承担什么责任”，不要根据颜色、括号、位置、空白或 Word 样式名单独
下结论。

## 五类责任

| 类别 | 含义 | 通常处理 |
| --- | --- | --- |
| 固定内容 | 校名、制度原文、固定标签、区域标题、表格骨架 | 保留文字与版式 |
| 条件固定内容 | 仅对某院系、语言、学位或条件成立 | 保留条件和证据；不自行裁决未知条件 |
| 学生填写位置 | 真实学生值或示例值所在的视觉位置 | 以最合适的对象物化 Registry 字段 |
| 说明/格式指导 | 告诉学生写什么、怎么排版、用后删除 | 提取其责任后清理，不把说明本身当填写值 |
| 生成内容 | TOC 缓存、自动编号、题注或交叉引用结果 | 保留生成机制，按来源对象刷新 |

无法证明时先标记未知并取证，而不是把“不确定”统一变成保留或删除。

## 判断顺序

1. 记录对象 ref、原文、样式、父子关系、文档顺序和所在页面。
2. 查看同页固定地标、标签和值的关系；必要时比较模板其他区域。
3. 查询 Registry 的 `label`、`meaning`、`content_type` 和 `cardinality`。词法匹配是候选，不是结论。
4. 检查要求文件是否明确该文字的身份或适用条件。
5. 页面布局参与语义时查看原生页面或局部图像。
6. 给出分类、证据、处理和仍存在的不确定性。

## 容易误判的情况

- 彩色/括号文字既可能是说明，也可能是固定正式名称或带格式注释的学生示例。
- `20 年 月 日` 可能是学生提交日期骨架；没有纯空白 run 不证明它是固定值。
- 声明页的手签/日期线是学校物理表单位置；Registry 没有专用字段时不要复用封面
  `submission.date`。
- 标签与学生值混在一段时，保留标签并选择承载值的最小安全 run；若年月日骨架不可安全拆分，
  整段可以是一个日期接口。
- 一段同时有导师姓名和职称两个空白时，它们是两个责任，不能因为共享段落而合并。
- 说明段和真实样例段描述同一责任时，只在真实内容版式上保留一个接口；不要双重物化。
- 同一字段可在封面和摘要页有两个真实视觉位置；“字段已出现”不能抵扣另一位置。
- 内容控件的可见 `【字段名】` 是既有填写界面，不是残留样文。

## 清理前闭合责任

删除说明或样例前，明确它证明的学生内容责任将由哪个保留对象承载。若删除后只剩固定标题、空容器
或无法填写的页面，说明责任尚未闭合。优先在真实样例对象上建立接口；没有样例时，才复用说明
所在的合适版式位置。
