# 目录

目录是由论文标题结构生成的特殊部件，不是普通段落流或一组逐行填写的文本槽位。

- 保留学校要求的目录标题、目录位置、页前分页和固定版式。
- 模板中的示例目录条目和示例页码不是最终内容；清理时不要把它们做成多个文本槽位。
- 槽位索引应把目录表示为一个可生成或更新的目录区域，并记录目录级别和学校要求的
  显示范围。
- 模板已经使用 Word 目录域时保留其结构；示例域结果可以在后续转换中更新。
- 手工输入的示例目录需要由后续转换生成真实目录，不能原样保留为最终目录。
- 图目录和表目录采用同一原则，但分别绑定图题和表题来源。
