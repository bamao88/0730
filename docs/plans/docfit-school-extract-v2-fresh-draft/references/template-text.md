# 模板文字

模板文字按它在最终模板中的作用处理，而不是按颜色、括号、字体或是否包含“填写”判断。

- 学校名称、表单标签、声明正文、固定标题和必须保留的提示属于模板固定内容。
- 只用于演示填写结果的姓名、日期、标题和示例段落应从干净模板中移除，并留下对应槽位。
- 指导作者如何填写的操作说明可以移除，但承载说明的表格、段落、间距和分页结构可能
  仍需保留。
- 同一句占位文字出现多次时，分别按所在论文部件和相邻标签建立槽位，不批量按文字替换。
- 无法确认是固定文字还是示例内容时先保留，并在槽位索引中记录未解决状态。
