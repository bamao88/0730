#!/usr/bin/env python3
"""Compile v2 template artifact decisions through the product compiler."""

from docfit.template.compile_artifact import compiler_main

if __name__ == "__main__":
    raise SystemExit(compiler_main())
