#!/usr/bin/env python3
"""Compile v2 template mutation decisions through the product compiler."""

from docfit.template.compile_mutation import compiler_main

if __name__ == "__main__":
    raise SystemExit(compiler_main())
